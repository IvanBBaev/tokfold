[package]
name = "tokfold-mcp"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "EXPERIMENTAL MCP stdio proxy library for tokfold. Not hardened; not for production."
keywords.workspace = true
categories.workspace = true
repository.workspace = true
homepage.workspace = true

[lints]
workspace = true

[dependencies]
# v0.0.1 is a placeholder with no proxy code, so nothing here calls into core yet.
# The edge is declared deliberately rather than dropped: the proxy is core's consumer
# by design, and the milestone that implements it starts from the real crate shape.
tokfold-core.workspace = true
